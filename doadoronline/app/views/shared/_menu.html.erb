<nav class="navbar navbar-default navbar-inverse navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="#" class="navbar-brand">Doador Online</a>
    </div>
    <!-- Collection of nav links and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= active_home %>"><%= link_to "Home", root_path %></li>
        <li class="<%= active_posts %>"><%= link_to "Post", posts_path %></li>
        <li class="<%= active_orders %>"><%= link_to "Pedido", orders_path %></li>
        <li class="<%= active_contacts %>"><%= link_to "Contato", contacts_path %></li>
      </ul>

      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to "Sair", destroy_user_session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Entrar", new_user_session_path %>
          </li>
        <% end %>

      </ul>

    </div>
  </div>
</nav>
